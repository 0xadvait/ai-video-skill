# Python deps for the seedance-video skill.
#   pip install -r requirements.txt
# review.py also needs ffmpeg + ffprobe on PATH (system install, not pip).
# veo-3.1 uses the stdlib only (REST) — no SDK required.
replicate>=1.0.0
fal-client>=0.5.0
